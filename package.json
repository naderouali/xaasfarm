{
  "name": "xaasfarm",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon start",
    "gitAdd": "git add . && git commit -m \"dev test\" && git push origin master",
    "heroku-postbuild": "react-scripts build && cd backend && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "bcrypt": "^5.0.0",
    "jsonwebtoken": "^8.5.1",
    "shortid": "^2.2.16",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.10",
    "nodemon": "^2.0.6"
  }
}
